import React from "react";

interface IProps {
}

const REPLACE_ME: React.FC<IProps> = (props: IProps): JSX.Element => {
    return (
        <React.Fragment>
            Homepage
        </React.Fragment>
    );
};

REPLACE_ME.defaultProps = {};

export default REPLACE_ME;